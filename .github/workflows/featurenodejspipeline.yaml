name: nodejs-feature-actions # name of the workflow 

# This defines the event that triggers the workflow
on: [push] # triggeres 

jobs:
  feature-pipeline:
    if: github.ref_name != 'main'
    uses: asifali-shaik/workflows/.github/workflows/nodejsfeaturepipeline.yaml@main
    with:
      project : 'git-hub'
      component: 'user'
      envrionment: 'dev'